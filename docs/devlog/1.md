## refactor plan 1

```rust
issues_page.next.is_none()
```

など、next().is_none()となっている箇所は、Iterator化して rust wayで書けるように思う。

## refactor plan 2

```rust
.filter(|issue| issue.pull_request.is_none()) // Filter out pull requests
```

のようになっている箇所はfilter_mapでsimpleに書ける　

## refactor plan 3

```rust
        repository_id: i64,
```

など、idとして扱われる型ははnew typeで定義して。
idは加算や除算可能であってはならない。

```rust
struct RepositoryId(i64)
```

また、上記の場合はi64ではなu64では?
数値型のidだけではなくString方のIDも同様
