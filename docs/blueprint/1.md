## この gitdbは次の機能を提供します

- github tokenは command実行時に"--github-token" optionで与えられます。 これが与えられない場合environment variable"GITDB_GITHUB_TOKEN"から読み込まれます。これが空の場合はtoken無しで使用します。

## 主なdependency

- octocrab: github api
-

### mcp & client

#### mcp serverとして

/bin/gitdb_mcp.rsおよび/tools以下に定義されます

### client commandとして

/bin/gitdb_cli.rs のbinが実行されます。

## directory構成　
